_target_: trainer.models.clip_model_mcdo.MCDropoutCLIPModel
pretrained_model_name_or_path: openai/clip-vit-large-patch14

# MC-Dropout controls
mc_dropout_p: 0.1            # 0 ≤ p < 1
apply_to: both               # one of: text | image | both
active_by_default: false     # keep training deterministic; enable via model.mc_dropout()
